Welcome back to Cable Modem Monitor!

Your (.venv) environment is active.

Background (on folder open - scripts/dev/sync_skills.sh):
  - skills/ synced to .claude/skills/ for Claude Code

Next steps:
  1. Run tests:
     Use VS Code Task: Ctrl+Shift+P -> Tasks -> 'Test: All' (or 'Test: Quick')
     Alternatively, run from terminal: make test

  2. Run code quality checks:
     Use VS Code Task: Ctrl+Shift+P -> Tasks -> 'Lint Code' (or 'Format Code')
     Alternatively, run from terminal: make lint / make format

  3. Start Home Assistant:
     Use VS Code Task: Ctrl+Shift+P -> Tasks -> 'HA: Start'
     (Then open http://localhost:8123)

  4. View all available tasks:
     Ctrl+Shift+P -> Tasks -> 'Run Task'

Happy coding!
