manufacturer: Netgear
model: CM1200
transport: http
default_host: "192.168.100.1"
timeout: 20
auth:
  strategy: basic
  challenge_cookie: true
hardware:
  docsis_version: "3.1"
  chipset: "Broadcom BCM3390"
status: awaiting_verification
sources:
  auth_config: "#121"
attribution:
  contributors:
    - github: DeFlanko
      contribution: "HAR captures and testing"
isps:
  - Comcast
  - Spectrum
  - Cox
notes: |
  DOCSIS 3.1 modem with SC-QAM and OFDM/OFDMA. HTTPS requires Basic Auth
  with challenge_cookie (401 sets XSRF_TOKEN). HTTP serves pages without auth.
  Timeout increased to 20s for HTTPS latency.
references:
  issues:
    - 63
    - 121
