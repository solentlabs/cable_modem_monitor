manufacturer: Netgear
model: CM1100
transport: http
default_host: "192.168.100.1"

auth:
  strategy: form
  action: "/goform/GenieLogin"
  login_page: "/"
  username_field: loginUsername
  password_field: loginPassword
  encoding: plain
  cookie_name: "SessionID"

hardware:
  docsis_version: "3.1"
  chipset: "Broadcom BCM3390"

status: awaiting_verification

sources:
  auth_config: "[#104 CM1100 HAR capture](https://github.com/solentlabs/cable_modem_monitor/issues/104)"

attribution:
  contributors:
    - github: dggrzesiak
      contribution: "HAR capture"

isps:
  - Comcast

notes: |
  DOCSIS 3.1 cable modem. Same tagValueList format as CM1200/CM2000.
  32 SC-QAM + OFDM downstream, ATDMA + OFDMA upstream.
  Login form includes webToken CSRF field. md5.js loaded but
  password sent plaintext in captured HAR.

references:
  issues:
    - "[#104 — [Modem Request] Netgear CM1100](https://github.com/solentlabs/cable_modem_monitor/issues/104)"
