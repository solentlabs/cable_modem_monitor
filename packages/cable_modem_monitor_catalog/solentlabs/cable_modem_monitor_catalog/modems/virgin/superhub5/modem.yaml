manufacturer: Virgin Media
model: Hub 5
model_aliases:
  - "SuperHub 5"
  - "VMDG660"
  - "F3896LG-VMB"
transport: http
default_host: "192.168.100.1"

auth:
  strategy: none

hardware:
  docsis_version: "3.1"
  chipset: "Broadcom BCM3390"

status: awaiting_verification

sources:
  chipset: "ISPreview"
  auth_config: "Synthetic HAR — no authentication required"

attribution:
  contributors:
    - github: Dinth
      contribution: "Initial Hub 5 support and REST API documentation"

isps:
  - Virgin Media UK

notes: |
  DOCSIS 3.1 gateway (OEM: Sagemcom F3896LG-VMB). REST API at
  /rest/v1/cablemodem/ — no authentication required.
  33 SC-QAM + 1 OFDM downstream, 5 ATDMA + 1 OFDMA upstream.
  Default IP: 192.168.100.1 (modem mode).

references:
  issues:
    - 82
