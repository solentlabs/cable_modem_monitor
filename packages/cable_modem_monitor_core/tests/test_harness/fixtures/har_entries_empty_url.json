{
  "_description": "HAR entry with empty URL — should be skipped by route builder",
  "_entries": [
    {
      "request": {"method": "GET", "url": ""},
      "response": {"status": 200, "content": {"text": "should not appear"}}
    }
  ]
}
