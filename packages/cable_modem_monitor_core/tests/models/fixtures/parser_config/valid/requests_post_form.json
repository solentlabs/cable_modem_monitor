{
  "requests": {
    "/status.html": {
      "method": "POST",
      "form": {"more": "1", "submit": "Show channels"}
    }
  },
  "downstream": {
    "format": "table",
    "resource": "/status.html",
    "tables": [
      {
        "selector": {"type": "nth", "match": 0},
        "columns": [
          {"index": 0, "field": "channel_id", "type": "integer"}
        ]
      }
    ]
  }
}
