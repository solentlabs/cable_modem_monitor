{
  "_description": "Column map: rewrites converted values to canonical form; unmapped values pass through",
  "_xml": "<root><downstream_table><downstream><mod>256qam</mod></downstream><downstream><mod>64qam</mod></downstream><downstream><mod>UNKNOWN</mod></downstream></downstream_table></root>",
  "_resource": "10",
  "_config": {
    "format": "xml",
    "tables": [
      {
        "resource": "10",
        "root_element": "downstream_table",
        "child_element": "downstream",
        "columns": [
          {
            "source": "mod",
            "field": "modulation",
            "type": "string",
            "map": {"256qam": "QAM256", "64qam": "QAM64"}
          }
        ]
      }
    ]
  },
  "_expected": [
    {"modulation": "QAM256"},
    {"modulation": "QAM64"},
    {"modulation": "UNKNOWN"}
  ]
}
