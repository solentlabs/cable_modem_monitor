# Modem metadata - verified against official sources
release_date: 2015
end_of_life: null # null = still current, or year
docsis_version: "3.0"
protocol: HTML
chipset: Broadcom BCM3384ZU # Per DeviWiki
isps:
  - Telia
source: null # No official product page found

# Fixture capture info
firmware_tested: null # Not visible in capture
captured_from_issue: 75
