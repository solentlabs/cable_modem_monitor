# Development dependencies for Cable Modem Monitor

# Testing framework
pytest>=7.4.3
pytest-cov>=4.1.0
pytest-asyncio>=0.21.1
pytest-homeassistant-custom-component>=0.13.0

# Code quality tools
ruff>=0.8.2
black>=23.11.0
pre-commit>=3.6.0
# Legacy linters (kept for compatibility, but ruff is preferred)
pylint>=3.0.0

# Type checking
mypy>=1.7.0
types-requests>=2.31.0.10

# Security scanning
bandit>=1.7.5

# Security-focused XML parsing (prevents XXE attacks)
defusedxml>=0.7.1

# Additional testing utilities
freezegun>=1.2.2
responses>=0.24.0
