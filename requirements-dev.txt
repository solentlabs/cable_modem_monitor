# Development dependencies for Cable Modem Monitor
# Used for local development, testing, linting, type checking, and security scanning

# -------------------------------
# 🏠 Home Assistant (install FIRST to resolve dependencies)
# -------------------------------
homeassistant>=2025.1.0                     # Home Assistant core (>= 2025.1.0 for pydantic v2)

# -------------------------------
# ⚙️ Runtime dependencies (mirrors pyproject.toml)
# -------------------------------
beautifulsoup4>=4.12.0,<5.0                 # HTML parsing
lxml>=5.0.0                                 # XML/HTML parser backend
requests>=2.31.0                            # HTTP client (HA determines version)
aiohttp>=3.9.0                              # Async HTTP client (HA determines version)

# -------------------------------
# 🧪 Testing framework and plugins
# -------------------------------
pytest-homeassistant-custom-component>=0.13.0,<1.0  # HA integration test harness (pins pytest, pytest-asyncio, syrupy, and all pytest plugins to exact versions)
pytest>=8.3.4                               # pinned by pytest-homeassistant-custom-component
pytest-cov>=6.0.0                           # pinned by pytest-homeassistant-custom-component
pytest-asyncio>=0.24.0                      # pinned by pytest-homeassistant-custom-component
pytest-mock>=3.12.0                         # Mocking utilities
pytest-socket>=0.7.0                        # Network access control for tests

# -------------------------------
# 🧹 Code quality and formatting
# -------------------------------
ruff>=0.15.13                               # Linter and import sorter
black>=24.0.0                               # Code formatter
pre-commit>=4.0.0                           # Git hook manager
pylint>=3.0.0                               # Optional static analysis

# -------------------------------
# 🔍 Type checking
# -------------------------------
mypy>=2.1.0                                 # Static type checker
types-requests>=2.31.0,<2.33.0              # Type stubs for requests; capped: 2.33+ requires urllib3>=2, HA 2025.1.x requires urllib3<2

# -------------------------------
# 🔐 Security scanning
# -------------------------------
bandit>=1.7.5                               # Static security analyzer
defusedxml>=0.7.1                           # Safer XML parsing

# -------------------------------
# 🧪 Additional testing utilities
# -------------------------------
freezegun>=1.5.0                            # Time-freezing for deterministic tests
responses>=0.26.0                           # Mock HTTP responses
