# =============================================================================
# AUTO-GENERATED FILE - DO NOT EDIT DIRECTLY
#
# Source: modems/arris/sb6190/modem.yaml
# This file is synced from modems/ during build. Edit the source file, then run:
#     make sync
# =============================================================================

# ARRIS SB6190 Configuration
# Source: Community contribution (sfennell, [PR #22](https://github.com/solentlabs/cable_modem_monitor/pull/22))

manufacturer: ARRIS
model: SB6190
paradigm: html

hardware:
  docsis_version: "3.0"
  chipset: Intel Puma 6
  release_date: "2016"

# Provenance tracking
sources:
  auth_config: "[Issue #83](https://github.com/solentlabs/cable_modem_monitor/issues/83)"
  detection_hints: "[PR #22](https://github.com/solentlabs/cable_modem_monitor/pull/22)"

protocol: http
default_host: "192.168.100.1"
default_port: 80

auth:
  # Default: no auth. Discovery overrides if firmware requires login.
  # - Older firmware: No auth required (public status page)
  # - Newer firmware (9.1.103+): Combined credential form auth
  # See: https://github.com/solentlabs/cable_modem_monitor/issues/83
  strategy: none

  # Ordered list of strategies to try (firmware variations)
  strategies:
    - strategy: none # Try first (older firmware - public page)
    - strategy: form # Try if none fails (newer firmware 9.1.103+)
      form:
        action: "/cgi-bin/login"
        method: POST
        username_field: "username"
        password_field: "password"
        password_encoding: plain

pages:
  public:
    - "/cgi-bin/status"

  protected: []

  data:
    downstream_channels: "/cgi-bin/status"
    upstream_channels: "/cgi-bin/status"

parser:
  class: ArrisSB6190Parser
  module: custom_components.cable_modem_monitor.modems.arris.sb6190.parser
  format:
    type: html
    table_layout: standard

detection:
  pre_auth:
    - "SB6190"
    - "ARRIS"
    - "Downstream Bonded Channels"
  post_auth:
    - "SB6190"
    - "ARRIS SB6190"
    - "Downstream Bonded Channels"
  page_hint: "/cgi-bin/status"

capabilities:
  - scqam_downstream
  - scqam_upstream

# =============================================================================
# STATUS
# =============================================================================
status_info:
  status: verified
  verification_source: "https://github.com/solentlabs/cable_modem_monitor/pull/22"

# =============================================================================
# METADATA
# =============================================================================
fixtures:
  path: modems/arris/sb6190/fixtures
  source: diagnostics
  firmware_tested: "*"
  last_validated: "2026-01-05"

attribution:
  contributors:
    - github: sfennell
      contribution: "[PR #22](https://github.com/solentlabs/cable_modem_monitor/pull/22) verification"
